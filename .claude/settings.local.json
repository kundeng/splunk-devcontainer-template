{
  "permissions": {
    "allow": [
      "Bash(git clone:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)"
    ]
  },
  "mcpServers": {
    "spec-workflow": {
      "command": "npx",
      "args": [
        "-y",
        "@pimzino/spec-workflow-mcp@latest",
        "/workspace"
      ]
    }
  }
}
