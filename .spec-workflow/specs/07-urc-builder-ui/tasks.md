# Tasks: URC Builder UI — Stream Dashboard & Schema-Driven Editor

## Phase A: Backend Foundation

- [x] 1. Add tags and debug fields to globalConfig.json, rebuild UCC output
  - Files: `ucc/urc_app/globalConfig.json`, `ucc/urc_app/package/bin/urc_app_input_helper.py`
  - Add `tags` (text, optional, default "") and `debug` (checkbox, optional, default false) fields to the `urc_app_input` service entity in globalConfig.json
  - Add `tags` and `debug` columns to the inputs table header and moreInfo sections
  - In `urc_app_input_helper.py`: read `debug` from input_item, set logger + `urc.engine` logger to DEBUG when `debug` is truthy
  - Run `task ucc:build` to regenerate output (verify the new RestField entries appear in the generated handler)
  - _Leverage: `ucc/urc_app/globalConfig.json`, `ucc/urc_app/package/bin/urc_app_input_helper.py`_
  - _Requirements: R9, R10_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Splunk UCC Developer | Task: Add `tags` and `debug` fields to the UCC input entity in globalConfig.json. The tags field is a text field for comma-separated values. The debug field is a checkbox that enables verbose logging. Also modify urc_app_input_helper.py to read the debug flag and set the Python logger to DEBUG level for that stream. Run `task ucc:build` to verify the generated REST handler includes the new fields. | Restrictions: Do not modify any other globalConfig sections (account, logging, proxy). Do not change the UCC build pipeline. Keep the debug logger scoping to `urc.engine` so it only affects the engine, not all of splunklib. | _Leverage: ucc/urc_app/globalConfig.json (existing input entity structure), ucc/urc_app/package/bin/urc_app_input_helper.py (existing log level setup at line 107-113) | _Requirements: R9.1, R9.2, R10.1, R10.2 | Success: `task ucc:build` succeeds. Generated handler at ucc/output/urc_app/bin/urc_app_rh_urc_app_input.py includes RestField entries for 'tags' and 'debug'. Input helper sets DEBUG level when debug flag is set. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 2. Activate manifest validation with capability detection
  - Files: `ucc/urc_app/package/lib/urc/validate.py`
  - Extend `validate_manifest_capabilities(manifest_yaml: str) -> list[dict]` that:
    - Calls `process_manifest()` to parse the YAML
    - Walks the manifest tree recursively, finds all `type` fields
    - Checks each type against `REGISTRY` — unknown types get `severity: "error"`
    - Checks for JWT with RS256/RS384/RS512/ES256/ES384/ES512 → `severity: "warning"` about cryptography dependency
    - Checks for OAuth2 with `code_challenge_method` / PKCE fields → `severity: "warning"`
    - Returns `[{path, type, severity, message}]`
  - Add unit tests in `ucc/urc_app/tests/unit/test_validate.py`
  - _Leverage: `ucc/urc_app/package/lib/urc/validate.py` (existing dead code), `ucc/urc_app/package/lib/urc/registry.py` (REGISTRY dict), `ucc/urc_app/package/lib/urc/manifest.py` (process_manifest)_
  - _Requirements: R8_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Python Backend Developer | Task: Extend validate.py with a `validate_manifest_capabilities()` function that walks a parsed manifest tree, checks every `type` field against the engine's REGISTRY, and detects unsupported auth patterns (JWT asymmetric algorithms, OAuth2 PKCE). Returns structured results as a list of dicts with path, type, severity, message fields. Write unit tests covering: unknown type detection, JWT RS256 warning, OAuth2 PKCE warning, valid manifest returns empty list. | Restrictions: Do not make HTTP requests in validation — this is purely structural analysis. Do not modify the existing `validate_manifest()` function, add the new function alongside it. Skip top-level types (DeclarativeSource, DeclarativeStream) that are structural, not component types. Import REGISTRY from urc.registry to check supported types. | _Leverage: ucc/urc_app/package/lib/urc/validate.py (existing Pydantic validation), ucc/urc_app/package/lib/urc/registry.py (REGISTRY dict with 50 registered types), ucc/urc_app/package/lib/urc/manifest.py (process_manifest for YAML parsing) | _Requirements: R8.1, R8.2, R8.3, R8.4, R8.5 | Success: `pytest ucc/urc_app/tests/unit/test_validate.py -v` passes. validate_manifest_capabilities returns correct results for manifests with unknown types, JWT RS256, OAuth2 PKCE, and valid manifests. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 3. Extend test_connection endpoint with validation mode
  - File: `ucc/urc_app/package/bin/urc_test_connection.py`
  - Add `mode` parameter support: `mode=validate` runs validation only (no HTTP), `mode=test` (default) runs validation + dry-run collection
  - Import and call `validate_manifest_capabilities()` from `urc.validate`
  - Include `validation` field in all response payloads
  - _Leverage: `ucc/urc_app/package/bin/urc_test_connection.py` (existing handler), `ucc/urc_app/package/lib/urc/validate.py` (task 2)_
  - _Requirements: R8.6_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Splunk REST Endpoint Developer | Task: Extend the existing TestConnectionHandler.handlePOST() method in urc_test_connection.py to support a `mode` parameter. When `mode=validate`, run only manifest validation (no network calls, no account lookup required) and return validation results. When `mode=test` (default), run validation first, then the existing dry-run collection. Include a `validation` field in all response payloads. | Restrictions: Do not break existing test_connection behavior — `mode=test` must produce identical results to current behavior plus the new validation field. When mode=validate, account_name is optional (skip credential lookup). Do not change the URL or HTTP method of the endpoint. | _Leverage: ucc/urc_app/package/bin/urc_test_connection.py (existing handler, lines 107-176), ucc/urc_app/package/lib/urc/validate.py (validate_manifest_capabilities from task 2) | _Requirements: R8.6 | Success: POST to /services/urc_app/test_connection with mode=validate returns validation results without network calls. POST with mode=test (or no mode) returns existing behavior plus validation field. Existing tests still pass. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 4. Add debug-level emit() and enrich AOP hooks
  - Files: `ucc/urc_app/package/lib/urc/structured_logger.py`, `ucc/urc_app/package/lib/urc/instrumentation.py`, `ucc/urc_app/package/lib/urc/engine.py`
  - Add `level` parameter to `emit()` — defaults to `"info"`, supports `"debug"`. Uses `logger.debug()` for debug level.
  - Modify `after_hook` to emit verbose context (url, stream_slice, stream_name) at DEBUG level when `logger.isEnabledFor(DEBUG)`
  - Add `before_hook` function that emits method entry at DEBUG level
  - Register `before_hook` in engine.py alongside existing hook registrations
  - Add/update unit tests in `ucc/urc_app/tests/unit/test_structured_logger.py`
  - _Leverage: `ucc/urc_app/package/lib/urc/structured_logger.py` (existing emit + hooks), `ucc/urc_app/package/lib/urc/instrumentation.py` (existing hook registration), `ucc/urc_app/package/lib/urc/engine.py` (hook registration block at lines 21-26)_
  - _Requirements: R9_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Python Observability Developer | Task: Add a `level` parameter to `emit()` in structured_logger.py that defaults to "info" and supports "debug". When level="debug", use logger.debug() instead of logger.info(). Modify after_hook to emit additional context fields (url, stream_slice, stream_name from the context dict) at DEBUG level, guarded by logger.isEnabledFor(DEBUG). Add a new before_hook function that emits action=enter at DEBUG level. Register before_hook in engine.py. Write tests verifying: debug-level messages only emit when logger is at DEBUG, INFO-level behavior is unchanged, before_hook fires correctly. | Restrictions: Do not change the behavior of existing INFO-level logging. The level check (logger.isEnabledFor) must happen BEFORE string formatting to avoid performance overhead. Do not modify any component files. Do not remove existing hooks. | _Leverage: ucc/urc_app/package/lib/urc/structured_logger.py (emit function at line 21, after_hook at line 38, error_hook at line 49), ucc/urc_app/package/lib/urc/engine.py (hook registration at lines 21-26) | _Requirements: R9.3, R9.4, R9.5 | Success: `pytest ucc/urc_app/tests/ -v` all pass. emit(level="debug") uses logger.debug(). after_hook emits context at DEBUG. before_hook emits entry at DEBUG. No change to INFO-level output. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

## Phase B: Schema-to-Form Generation

- [x] 5. Create build-time schema-to-form generator script
  - Files: `react/packages/urc-builder/scripts/generate-form-schema.js`, `react/packages/urc-builder/src/schema/form-schema.ts` (generated output)
  - Write a Node.js script that:
    - Reads `ucc/urc_app/schema/declarative_component_schema.yaml` via `js-yaml`
    - Walks `definitions`, categorizes components (authenticator, paginator, extractor, transformation, error_handler, cursor, decoder)
    - For each component: extracts title, description, required fields, property definitions (title, description, type, enum, default, examples, $ref resolution)
    - Checks type against a hardcoded SUPPORTED_TYPES list (mirroring Python REGISTRY)
    - Outputs `form-schema.ts` with typed exports: `AUTHENTICATORS`, `PAGINATORS`, `EXTRACTORS`, `TRANSFORMATIONS`, `ERROR_HANDLERS`, `INCREMENTAL_CURSORS`, `DECODERS`
  - Add `prebuild` script to `react/packages/urc-builder/package.json`
  - Add `js-yaml` as devDependency
  - Also reads `urc_extensions_schema.yaml` for EventTimestamp types
  - _Leverage: `ucc/urc_app/schema/declarative_component_schema.yaml`, `ucc/urc_app/schema/urc_extensions_schema.yaml`_
  - _Requirements: R4_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Node.js Build Tool Developer | Task: Create a Node.js script at react/packages/urc-builder/scripts/generate-form-schema.js that reads the Airbyte declarative_component_schema.yaml and urc_extensions_schema.yaml, processes all component definitions, and generates a TypeScript file (form-schema.ts) with typed form field definitions grouped by category. Each ComponentFormDef includes: type, category, label, description, fields array (with key, label, helpText, type, required, enumValues, defaultValue, examples, nestedFields), and a supported boolean. Add js-yaml as a devDependency and a prebuild script. | Restrictions: The generated file must be valid TypeScript that compiles without errors. Do not include the full YAML content in the output — extract only what the form renderer needs. Keep the output under 2000 lines. Resolve $ref pointers within the schema to inline nested object definitions (max 2 levels deep). Use the interfaces defined in the design doc (FormFieldDef, ComponentFormDef). | _Leverage: ucc/urc_app/schema/declarative_component_schema.yaml (component definitions with titles, descriptions, enums, defaults), ucc/urc_app/schema/urc_extensions_schema.yaml (EventTimestamp types) | _Requirements: R4.2, R4.3, R4.4, R4.5 | Success: Running `node scripts/generate-form-schema.js` produces a valid form-schema.ts. The file exports typed arrays for each component category. `yarn build` in urc-builder succeeds with the generated file. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

## Phase C: Core React Infrastructure

- [x] 6. Create shared types, Splunk API service, and manifest serializer
  - Files: `react/packages/urc-builder/src/types.ts`, `react/packages/urc-builder/src/services/splunk-api.ts`, `react/packages/urc-builder/src/services/manifest-serializer.ts`
  - `types.ts`: TypeScript interfaces — InputSummary, SavedInput, ValidationResult, TestResult, StreamConfig, BuilderState, BuilderAction, FormFieldDef, ComponentFormDef
  - `splunk-api.ts`: REST client using `@splunk/splunk-utils` createRESTURL + fetch. Methods: listInputs, getInput, createInput, updateInput, deleteInput, toggleInput, listAccounts, listIndexes, testConnection, validateManifest. Handles Splunk's JSON response envelope format.
  - `manifest-serializer.ts`: `formStateToManifest(state) → YAML string`, `manifestToFormState(yaml, defaults) → partial state`. Bidirectional mapping between flat form state and nested Airbyte manifest structure.
  - _Leverage: `@splunk/splunk-utils` (createRESTURL), existing REST endpoint paths from design doc_
  - _Requirements: R2, R5, R8_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: TypeScript Frontend Developer | Task: Create three foundational modules for the URC builder. (1) types.ts with all TypeScript interfaces from the design doc (InputSummary, SavedInput, ValidationResult, TestResult, StreamConfig, BuilderState, BuilderAction, FormFieldDef, ComponentFormDef). (2) splunk-api.ts — REST client wrapping @splunk/splunk-utils createRESTURL with fetch(). Must handle Splunk's response envelope (the `entry` array pattern). Methods: listInputs, getInput, createInput, updateInput, deleteInput, toggleInput, listAccounts, listIndexes, testConnection (calls /services/urc_app/test_connection), validateManifest (calls test_connection with mode=validate). All methods add output_mode=json. (3) manifest-serializer.ts — bidirectional converter between form state and Airbyte manifest YAML. formStateToManifest serializes streams array with retriever, pagination, incremental_sync etc. manifestToFormState parses YAML and populates a BuilderState. Add js-yaml as a dependency. | Restrictions: Do not use axios or any HTTP library beyond fetch. Do not hardcode Splunk URLs — use createRESTURL for all endpoint construction. The manifest serializer must handle round-trips losslessly for all supported component types. Account and base_url are UCC input fields, NOT part of the manifest YAML. | _Leverage: @splunk/splunk-utils (createRESTURL for building proxied Splunk REST URLs), design doc data models section, design doc REST endpoint paths | _Requirements: R2.3, R2.4, R5.1, R5.2 | Success: Types compile without errors. splunk-api.ts exports all listed methods with correct TypeScript signatures. manifest-serializer round-trip test: create a BuilderState, serialize to YAML, parse back — values match. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 7. Create BuilderContext and DashboardContext
  - Files: `react/packages/urc-builder/src/context/BuilderContext.tsx`, `react/packages/urc-builder/src/context/DashboardContext.tsx`
  - `BuilderContext`: React Context + useReducer with BuilderState/BuilderAction types. Actions: SET_FIELD (dot-path updates), LOAD_INPUT (parse existing input into form state), SET_TEST_RESULT, SET_VALIDATION, RESET. Provider component wraps StreamBuilder.
  - `DashboardContext`: React Context for dashboard state — inputs list, selectedTags, selectedStatus filter, selectedRows for bulk actions, loading state. Provider wraps StreamDashboard.
  - _Leverage: React 18 Context API, types from task 6_
  - _Requirements: R2, R1_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React State Management Developer | Task: Create two React Context providers. (1) BuilderContext.tsx — useReducer with BuilderState and BuilderAction types from types.ts. The reducer handles: SET_FIELD (uses a dot-path string like "streams[0].retriever.type" to update nested state via a helper function), LOAD_INPUT (calls manifestToFormState to populate state from a saved input), SET_TEST_RESULT (stores test results), SET_VALIDATION (stores validation results), RESET (returns to initial state). Export BuilderProvider, useBuilder hook, and initial state. (2) DashboardContext.tsx — simpler context for dashboard filters. State: inputs array, selectedTags, selectedStatus, selectedRows, loading. Export DashboardProvider and useDashboard hook. | Restrictions: Do not use Redux, MobX, or any external state library. Use React.useReducer for BuilderContext and React.useState for DashboardContext (it's simpler). The SET_FIELD action must handle nested dot-path updates correctly (e.g., "streams[0].retriever.paginator.type" updates deeply nested state). Do not store derived state — compute filters in selectors. | _Leverage: react/packages/urc-builder/src/types.ts (BuilderState, BuilderAction interfaces from task 6), react/packages/urc-builder/src/services/manifest-serializer.ts (manifestToFormState for LOAD_INPUT action) | _Requirements: R2.2 (state preservation across tabs), R1.3 (tag filtering) | Success: BuilderContext reducer correctly handles all action types. Dot-path SET_FIELD updates nested state. LOAD_INPUT populates form from manifest YAML. DashboardContext filters work correctly. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

## Phase D: Stream Dashboard (Screen 1)

- [-] 8. Build StreamDashboard, StreamTable, TagFilter, SummaryCards, BulkActions
  - Files: `react/packages/urc-builder/src/dashboard/StreamDashboard.tsx`, `react/packages/urc-builder/src/dashboard/StreamTable.tsx`, `react/packages/urc-builder/src/dashboard/TagFilter.tsx`, `react/packages/urc-builder/src/dashboard/SummaryCards.tsx`, `react/packages/urc-builder/src/dashboard/BulkActions.tsx`
  - `StreamDashboard`: Top-level component. On mount, calls `listInputs()`. Wraps children in DashboardProvider. Renders SummaryCards, TagFilter, BulkActions, StreamTable.
  - `StreamTable`: Splunk `<Table>` component with columns: Name, Tags (as Chips), Account, Status (badge), Interval. Row click navigates to `/app/urc_app/builder?input=<name>`. Row selection via checkboxes for bulk actions.
  - `TagFilter`: Extract unique tags from all inputs, render as `<Chip>` components. Click toggles filter. "All" option clears filters.
  - `SummaryCards`: Three cards — Total Streams, Active, Errors. Computed from inputs list.
  - `BulkActions`: Appears when rows are selected. Buttons: Enable, Disable, Delete (with confirmation Modal), Set Tags.
  - _Leverage: `@splunk/react-ui` Table, Chip, Button, Modal, Message, ColumnLayout, Heading, WaitSpinner. `splunk-api.ts` from task 6. `DashboardContext` from task 7._
  - _Requirements: R1_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React Frontend Developer specializing in Splunk UI | Task: Build the Stream Dashboard (Screen 1) as five components. StreamDashboard is the top-level container that fetches inputs on mount via splunk-api.ts listInputs() and wraps everything in DashboardProvider. StreamTable renders a Splunk Table component with columns for Name, Tags (as Chip components), Account, Status (green/red/gray badge), and Interval. Rows are clickable (navigate to builder page) and selectable via checkboxes. TagFilter extracts unique tags from all inputs and renders them as clickable Chip components for filtering. SummaryCards shows three count cards (Total, Active, Errors) using ColumnLayout. BulkActions shows Enable/Disable/Delete buttons when rows are selected, with a confirmation Modal for Delete. | Restrictions: Use ONLY @splunk/react-ui components — do not install additional UI libraries. Navigation to builder uses window.location (Splunk doesn't use React Router — each page is a separate webpack entry point). Status is derived from the `disabled` field (not runtime state — we don't have runtime status in UCC's REST response yet). Style with styled-components using @splunk/themes variables for light/dark theme support. | _Leverage: @splunk/react-ui (Table, Chip, Button, Modal, Message, ColumnLayout, Heading, WaitSpinner), react/packages/urc-builder/src/services/splunk-api.ts (listInputs, deleteInput, toggleInput), react/packages/urc-builder/src/context/DashboardContext.tsx | _Requirements: R1.1, R1.2, R1.3, R1.4, R1.5, R1.6, R1.7 | Success: Dashboard renders a table of inputs fetched from UCC API. Tag filter chips appear and filter the table. Summary cards show correct counts. Bulk enable/disable/delete works via API calls. Row click navigates to builder. Light and dark themes both render correctly. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 9. Create streams page entry point and Splunk view registration
  - Files: `react/packages/urc-app/src/main/webapp/pages/streams/index.tsx`, `react/packages/urc-app/src/main/webapp/pages/streams/Styles.ts`, `react/packages/urc-app/src/main/resources/splunk/default/data/ui/views/streams.xml`, `react/packages/urc-app/src/main/resources/splunk/appserver/templates/streams.html`, `react/packages/urc-app/src/main/resources/splunk/default/data/ui/nav/default.xml`
  - `streams/index.tsx`: Page entry point — same pattern as existing `builder/index.tsx`. Uses `@splunk/react-page` layout, loads theme, mounts `<StreamDashboard />`.
  - `streams.xml`: View definition pointing to `streams.html` template.
  - `streams.html`: Same structure as `builder.html` — loads Splunk config, i18n, page JS.
  - Update `default.xml` nav: add `streams` as default view, keep `builder` and `configuration`.
  - _Leverage: `react/packages/urc-app/src/main/webapp/pages/builder/index.tsx` (existing pattern), `react/packages/urc-app/src/main/resources/splunk/` (existing view/nav/template structure)_
  - _Requirements: R1_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Splunk React App Developer | Task: Create the "streams" page entry point following the exact same pattern as the existing builder page. Create index.tsx that imports StreamDashboard from @splunk/urc-builder, wraps it in a styled container, and mounts via @splunk/react-page layout(). Create the Splunk view XML, HTML template, and update navigation. The streams page should be the new default landing page. | Restrictions: Follow the exact pattern of the existing builder/index.tsx — do not introduce new patterns. The HTML template must match the structure of builder.html (Splunk config loading, i18n, page JS bundle). Keep the builder view in nav (it's accessed via URL params from streams). Keep configuration view in nav. | _Leverage: react/packages/urc-app/src/main/webapp/pages/builder/index.tsx (exact pattern to follow), react/packages/urc-app/src/main/resources/splunk/ (existing view/nav/template structure to mirror) | _Requirements: R1.1 | Success: `yarn build` in urc-app produces a streams.js bundle in stage/appserver/static/pages/. Navigation XML has streams as default. View XML and HTML template follow existing patterns. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

## Phase E: Stream Builder (Screen 2) — Shell & Tabs

- [ ] 10. Build StreamBuilder tabbed container and ConnectionTab
  - Files: `react/packages/urc-builder/src/builder/StreamBuilder.tsx`, `react/packages/urc-builder/src/builder/tabs/ConnectionTab.tsx`
  - `StreamBuilder`: Top-level builder component. Reads `?input=<name>` from URL to determine create vs edit mode. On edit: fetches input via `getInput()`, dispatches LOAD_INPUT. Renders `<TabLayout>` with 5 tabs. Renders Save/Cancel buttons in a sticky footer. Save: calls `formStateToManifest()`, then `createInput()` or `updateInput()`.
  - `ConnectionTab`: Account dropdown (from `listAccounts()`), Base URL text field, HTTP Method selector (GET/POST). Read-only auth type summary below account dropdown. All fields wired to BuilderContext via SET_FIELD.
  - Wrap in BuilderProvider.
  - _Leverage: `@splunk/react-ui` TabLayout, Select, Text, ControlGroup, Button, Message. `BuilderContext` from task 7. `splunk-api.ts` from task 6. `manifest-serializer.ts` from task 6._
  - _Requirements: R2, R3_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React Form Developer specializing in Splunk UI | Task: Build the StreamBuilder shell component and ConnectionTab. StreamBuilder reads the URL query param `input` to determine create vs edit mode. In edit mode, it fetches the input via getInput() and dispatches LOAD_INPUT to populate form state. It renders a TabLayout with 5 tab labels (Connection, Data Mapping, Splunk Output, Schedule & Tags, Test & Preview). Only implement ConnectionTab content in this task — other tabs render placeholder text. ConnectionTab renders: Account dropdown (populated from listAccounts()), Base URL text input, HTTP Method select (GET/POST). Below the account dropdown, show a read-only text line summarizing the auth type (e.g., "Auth: API Key via X-API-Key header"). Save button in a sticky footer serializes form state to manifest YAML and calls createInput or updateInput. Cancel navigates back to streams page. | Restrictions: Use TabLayout from @splunk/react-ui, not a custom tab implementation. Form fields must dispatch SET_FIELD actions to BuilderContext — no local state for form values. The Save button must be in a sticky footer that's always visible regardless of scroll position. Account dropdown must show account names only — never expose encrypted credentials. | _Leverage: @splunk/react-ui (TabLayout, Select, Text, ControlGroup, Button, Message), react/packages/urc-builder/src/context/BuilderContext.tsx, react/packages/urc-builder/src/services/splunk-api.ts (getInput, listAccounts, createInput, updateInput), react/packages/urc-builder/src/services/manifest-serializer.ts (formStateToManifest, manifestToFormState) | _Requirements: R2.1, R2.2, R2.3, R2.4, R3.1, R3.2, R3.3 | Success: TabLayout renders 5 tabs. ConnectionTab shows account dropdown and base URL. Edit mode populates fields from saved input. Save serializes and calls UCC API. Tab switching preserves form state. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [ ] 11. Build SchemaSection, SchemaField, TypeSelector, ValidationBanner
  - Files: `react/packages/urc-builder/src/builder/form/SchemaSection.tsx`, `react/packages/urc-builder/src/builder/form/SchemaField.tsx`, `react/packages/urc-builder/src/builder/form/TypeSelector.tsx`, `react/packages/urc-builder/src/builder/form/NestedObjectField.tsx`, `react/packages/urc-builder/src/builder/form/ValidationBanner.tsx`
  - `SchemaSection`: Takes a `ComponentFormDef`, renders a `<CollapsiblePanel>` with title and description. Inside: a `TypeSelector` for the component type, then `SchemaField` for each field in the selected type's definition.
  - `SchemaField`: Dispatches rendering by field type — text→Text, number→Number, boolean→Switch, enum→Select, template→Text with indicator, object→NestedObjectField, array→list with add/remove.
  - `TypeSelector`: Select dropdown showing component types for a category. Groups into "Supported" and "Unsupported". Unsupported types are selectable but trigger ValidationBanner.
  - `NestedObjectField`: Recursively renders SchemaField for each nested field in a $ref object.
  - `ValidationBanner`: Renders a `<Message type="warning">` or `<Message type="error">` for validation results matching the current section path.
  - _Leverage: `@splunk/react-ui` CollapsiblePanel, Select, Text, Number, Switch, ControlGroup, Message. `form-schema.ts` from task 5._
  - _Requirements: R4, R5_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React Form Component Developer | Task: Build the generic, schema-driven form rendering system as 5 components. SchemaSection renders a CollapsiblePanel with a TypeSelector and the fields for the selected type. SchemaField is a switch component that renders the appropriate @splunk/react-ui input based on field type: text→Text, number→Number, boolean→Switch, enum→Select with options from enumValues, template→Text with a small "{{ }}" badge, object→NestedObjectField, array→list with add/remove buttons. TypeSelector renders a Select dropdown grouped into "Supported" and "Unsupported" sections — unsupported types show a warning icon. NestedObjectField recursively renders SchemaField for each property in a $ref-resolved object. ValidationBanner renders Message components for validation results matching the section's manifest path. All field changes dispatch SET_FIELD to BuilderContext. | Restrictions: Must be generic — these components know nothing about specific API types, only about FormFieldDef/ComponentFormDef shapes. Do not hardcode field names. Help text from the schema (helpText field) must appear below each form field via ControlGroup's help prop. Limit recursion depth to 3 levels for nested objects. Use @splunk/react-ui components exclusively. | _Leverage: react/packages/urc-builder/src/schema/form-schema.ts (ComponentFormDef, FormFieldDef types from task 5), @splunk/react-ui (CollapsiblePanel, Select, Text, Number, Switch, ControlGroup, Message, Button), react/packages/urc-builder/src/context/BuilderContext.tsx (SET_FIELD dispatch) | _Requirements: R4.1, R4.2, R4.3, R4.4, R4.5, R4.6 | Success: SchemaSection renders fields for any ComponentFormDef. TypeSelector shows supported/unsupported grouping. SchemaField renders correct input for each type. ValidationBanner shows warnings for unsupported types. All fields dispatch to BuilderContext. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [ ] 12. Build DataMappingTab using schema-driven form components
  - File: `react/packages/urc-builder/src/builder/tabs/DataMappingTab.tsx`
  - Renders collapsible sections for: Record Selector (extractors), Pagination (paginators), Incremental Sync (cursors), Transformations (array of transformation sections), Error Handling.
  - Each section uses `<SchemaSection>` with the appropriate category from `form-schema.ts`.
  - Transformations section supports adding/removing multiple transformations (array with add button).
  - Wired to BuilderContext — reads/writes `state.streams[0]` (single-stream for MVP, multi-stream later).
  - _Leverage: SchemaSection, TypeSelector from task 11. form-schema.ts exports (EXTRACTORS, PAGINATORS, etc.) from task 5._
  - _Requirements: R4_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React Form Composition Developer | Task: Build DataMappingTab that composes multiple SchemaSection components for each part of the manifest's stream definition. Sections: Record Selector (uses EXTRACTORS from form-schema), Pagination (uses PAGINATORS), Incremental Sync (uses INCREMENTAL_CURSORS), Transformations (uses TRANSFORMATIONS — this is an array, so render multiple SchemaSection instances with add/remove buttons), Error Handling (uses ERROR_HANDLERS). Each section reads/writes to BuilderContext state at the appropriate path (e.g., streams[0].retriever.recordSelector, streams[0].retriever.paginator, etc.). For MVP, only support a single stream (streams[0]) — multi-stream support comes later. | Restrictions: Do not duplicate schema-driven rendering logic — delegate entirely to SchemaSection/SchemaField. The tab must work with an empty/new stream (all sections in default state) and with a populated stream (loaded from manifest). Transformations must support an ordered list — user can add, remove, and the order matters. | _Leverage: react/packages/urc-builder/src/builder/form/SchemaSection.tsx (task 11), react/packages/urc-builder/src/schema/form-schema.ts (EXTRACTORS, PAGINATORS, INCREMENTAL_CURSORS, TRANSFORMATIONS, ERROR_HANDLERS from task 5), react/packages/urc-builder/src/context/BuilderContext.tsx | _Requirements: R4.1, R4.2 | Success: DataMappingTab renders all 5 sections. Selecting a type in any section renders the correct fields. Transformation array add/remove works. Values persist in BuilderContext across tab switches. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

## Phase F: Remaining Builder Tabs

- [ ] 13. Build SplunkOutputTab with timestamp picker
  - Files: `react/packages/urc-builder/src/builder/tabs/SplunkOutputTab.tsx`, `react/packages/urc-builder/src/builder/test/TimestampPicker.tsx`
  - `SplunkOutputTab`: Index dropdown (from `listIndexes()`), Sourcetype text (default `urc:api:json`), Source override (optional), Host override (optional). Timestamp section: if test results exist in BuilderContext, show TimestampPicker; otherwise show "Run a test fetch to detect timestamp fields" with link to Tab 5.
  - `TimestampPicker`: Analyzes test result records. Detects datetime candidates by field name heuristic (`*_at`, `*_date`, `*_time`, `timestamp`, `created`, `updated`, `modified`) and value heuristic (`Date.parse()`, epoch detection). Renders radio buttons with field name, sample value, parsed preview. Selection dispatches SET_FIELD for timestampField.
  - _Leverage: `@splunk/react-ui` Select, Text, RadioBar, ControlGroup. `splunk-api.ts` listIndexes(). BuilderContext test results._
  - _Requirements: R5_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React Data Display Developer | Task: Build SplunkOutputTab and TimestampPicker. SplunkOutputTab renders: Index dropdown populated from listIndexes(), Sourcetype text input (default urc:api:json), optional Source and Host override text inputs. Below those, a Timestamp section that conditionally renders TimestampPicker (when test results exist in BuilderContext) or a message linking to the Test tab. TimestampPicker analyzes sample records from test results: detect datetime candidate fields by name heuristic (*_at, *_date, *_time, timestamp, created, updated, modified) and value heuristic (Date.parse succeeds, epoch integers > 946684800). Render each candidate as a RadioBar option showing field name, a sample value from the first record, and the parsed Date preview. Selection updates state.timestampField via SET_FIELD. | Restrictions: TimestampPicker must handle records with no datetime fields gracefully (show "No timestamp candidates detected"). Index dropdown must handle Splunk API errors (show Message with retry). Do not auto-select a timestamp field — let the user choose explicitly. | _Leverage: @splunk/react-ui (Select, Text, RadioBar, ControlGroup, Message), react/packages/urc-builder/src/services/splunk-api.ts (listIndexes), react/packages/urc-builder/src/context/BuilderContext.tsx (testResult state, SET_FIELD dispatch) | _Requirements: R5.1, R5.2, R5.3, R5.4, R5.5 | Success: SplunkOutputTab renders index dropdown and text fields. TimestampPicker correctly detects datetime fields from test results. Radio selection updates BuilderContext. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [ ] 14. Build ScheduleTagsTab
  - File: `react/packages/urc-builder/src/builder/tabs/ScheduleTagsTab.tsx`
  - Interval: Number input (10-86400 seconds) with ControlGroup validation
  - Tags: Text input for comma-separated values
  - Debug Logging: Switch toggle
  - Enabled/Disabled: Switch toggle
  - All fields wired to BuilderContext via SET_FIELD
  - _Leverage: `@splunk/react-ui` Number, Text, Switch, ControlGroup._
  - _Requirements: R6, R9_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React Form Developer | Task: Build ScheduleTagsTab with four form sections. Interval: Number input (min 10, max 86400) with help text "Collection interval in seconds". Tags: Text input with help text "Comma-separated tags for organizing streams (e.g. devops, security)". Debug Logging: Switch toggle with help text "Enable verbose debug logging for this stream". Enabled: Switch toggle with help text "Enable or disable this stream". All fields dispatch SET_FIELD to BuilderContext (interval, tags, debug, enabled paths). | Restrictions: Use @splunk/react-ui Number component with min/max validation, not a plain text input for interval. Keep the layout clean — use ControlGroup for each field with labels and help text. | _Leverage: @splunk/react-ui (Number, Text, Switch, ControlGroup), react/packages/urc-builder/src/context/BuilderContext.tsx | _Requirements: R6.1, R6.2, R9.1 | Success: All four fields render correctly and dispatch to BuilderContext. Interval validates min/max. Values persist across tab switches. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [ ] 15. Build TestPreviewTab with TestPanel, RecordPreview, FieldDetector
  - Files: `react/packages/urc-builder/src/builder/tabs/TestPreviewTab.tsx`, `react/packages/urc-builder/src/builder/test/TestPanel.tsx`, `react/packages/urc-builder/src/builder/test/RecordPreview.tsx`, `react/packages/urc-builder/src/builder/test/FieldDetector.tsx`
  - `TestPreviewTab`: Container rendering TestPanel, and when results exist: RecordPreview + FieldDetector side by side.
  - `TestPanel`: "Test Connection" button. On click: serializes current form state to manifest YAML, calls `testConnection()` from splunk-api. Shows WaitSpinner during request. On success: dispatches SET_TEST_RESULT and SET_VALIDATION. Shows success/error Message. For non-GET methods, shows confirmation dialog before test.
  - `RecordPreview`: Renders sample records as formatted JSON using `<Code>` component or JSONTree.
  - `FieldDetector`: Lists all detected fields from sample records with inferred types (string, number, boolean, object, array, datetime).
  - Results are cached in BuilderContext (not re-fetched on tab switch).
  - _Leverage: `@splunk/react-ui` Button, WaitSpinner, Message, Modal, Code, ColumnLayout. `splunk-api.ts` testConnection(). BuilderContext SET_TEST_RESULT._
  - _Requirements: R7_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React Data Visualization Developer | Task: Build TestPreviewTab with three sub-components. TestPanel renders a "Test Connection" button. When clicked, it serializes the current BuilderContext form state to manifest YAML via formStateToManifest(), then calls testConnection() from splunk-api. During the request, show WaitSpinner. On success: dispatch SET_TEST_RESULT and SET_VALIDATION to BuilderContext, show a success Message with record count. On error: show error Message with actionable text (e.g., "401 Unauthorized — check credentials in Configuration → Accounts"). For non-GET HTTP methods, show a Modal confirmation before executing ("This test will make a POST request to the API. Proceed?"). RecordPreview renders sample records using @splunk/react-ui Code component with JSON formatting. FieldDetector analyzes all records, lists unique field names with inferred types (string, number, boolean, object, array, datetime). Results cached in BuilderContext — tab switching does not re-fetch. | Restrictions: Never auto-run tests — always require explicit button click. Cache results in BuilderContext state, not in component-local state (so they survive tab switches). Limit JSON preview to first 5 records. Truncate very long field values in display (>200 chars). | _Leverage: @splunk/react-ui (Button, WaitSpinner, Message, Modal, Code, ColumnLayout), react/packages/urc-builder/src/services/splunk-api.ts (testConnection), react/packages/urc-builder/src/services/manifest-serializer.ts (formStateToManifest), react/packages/urc-builder/src/context/BuilderContext.tsx (SET_TEST_RESULT, SET_VALIDATION) | _Requirements: R7.1, R7.2, R7.3, R7.4, R7.5, R7.6 | Success: Test button triggers API call and shows results. Non-GET methods show confirmation. Results display as formatted JSON. Field list shows types. Results persist across tab switches. Errors show actionable messages. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

## Phase G: Update Builder Page Entry Point

- [ ] 16. Update builder page entry point to mount StreamBuilder
  - File: `react/packages/urc-app/src/main/webapp/pages/builder/index.tsx`
  - Replace the placeholder UrcBuilder import with StreamBuilder from `@splunk/urc-builder`
  - Pass URL query params to StreamBuilder for edit mode detection
  - _Leverage: existing `builder/index.tsx` pattern_
  - _Requirements: R2_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Splunk React App Developer | Task: Update the existing builder page entry point to mount StreamBuilder instead of the placeholder UrcBuilder component. Read URL query parameter `input` from window.location.search and pass it as a prop to StreamBuilder for determining create vs edit mode. | Restrictions: Follow the exact same pattern as the existing code — getUserTheme, layout(), styled container. Do not change the HTML template or view XML. | _Leverage: react/packages/urc-app/src/main/webapp/pages/builder/index.tsx (existing pattern to modify) | _Requirements: R2.1 | Success: Builder page renders StreamBuilder with TabLayout. Edit mode (with ?input=name) loads existing input. Create mode (no param) shows empty form. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

## Phase H: Build, Test, Validate

- [ ] 17. Update urc-builder exports, shared styles, and ensure build succeeds
  - Files: `react/packages/urc-builder/src/index.ts`, `react/packages/urc-builder/src/styles/shared.ts`, `react/packages/urc-builder/package.json`, `react/packages/urc-app/package.json`
  - Update `index.ts` to export StreamDashboard, StreamBuilder, and all context providers
  - Create shared styled-components using @splunk/themes variables
  - Add `js-yaml` as dependency in urc-builder package.json
  - Ensure `yarn build` succeeds in both packages
  - Run `yarn lint` and fix any issues
  - _Leverage: existing package.json scripts and webpack configs_
  - _Requirements: all_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Build Engineer | Task: Wire everything together. Update urc-builder/src/index.ts to export StreamDashboard, StreamBuilder, BuilderProvider, DashboardProvider, and all public types. Create styles/shared.ts with common styled-components (containers, section wrappers, sticky footer) using @splunk/themes variables. Add js-yaml as a runtime dependency. Ensure yarn build succeeds in both urc-builder and urc-app packages. Run yarn lint and fix issues. | Restrictions: Do not modify webpack configs unless absolutely necessary. Do not add dependencies beyond js-yaml. Remove the old placeholder UrcBuilder component and its tests. | _Leverage: react/packages/urc-builder/src/index.ts (existing barrel file), react/packages/urc-builder/package.json, react/packages/urc-app/package.json | _Requirements: all NFRs | Success: yarn build succeeds in both packages. yarn lint passes. StreamDashboard and StreamBuilder are importable from @splunk/urc-builder. Stage output contains both streams.js and builder.js page bundles. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [ ] 18. Run full backend test suite, build UCC, run appinspect
  - Run `pytest ucc/urc_app/tests/ -v` — all tests pass (including new validation tests)
  - Run `task ucc:build` — succeeds with new globalConfig fields
  - Run `task ucc:appinspect` — 0 errors, 0 failures
  - Fix any issues found
  - _Requirements: all_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: QA Engineer | Task: Run the full validation suite. (1) pytest ucc/urc_app/tests/ -v — all tests must pass including the new test_validate.py and updated test_structured_logger.py. (2) task ucc:build — must succeed with the updated globalConfig.json (tags and debug fields). (3) task ucc:appinspect — must return 0 errors and 0 failures. Fix any issues found in each step before proceeding to the next. | Restrictions: Do not skip failing tests — fix them. Do not modify appinspect rules. | _Leverage: existing Taskfile tasks (ucc:build, ucc:appinspect), pytest config | _Requirements: all | Success: All pytest tests pass. UCC build succeeds. Appinspect returns 0 errors, 0 failures. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [ ] 19. Commit and push all changes
  - Git add all new and modified files
  - Commit with descriptive message
  - Push to origin feature/urc-app
  - _Requirements: all_
  - _Prompt: Implement the task for spec 07-urc-builder-ui, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Developer | Task: Stage all new and modified files (backend and frontend), create a commit with message describing the URC Builder UI implementation, and push to origin feature/urc-app. | Restrictions: Do not push __pycache__ or node_modules. Do not amend previous commits. Review staged files before committing. | Success: All changes committed and pushed to origin. Mark task [-] in tasks.md before starting, log implementation with log-implementation tool after completion, then mark [x] when done._
